package src._1_string;

public class str1_12 {
    // for문을 cnt번 반복하고
    // 7의 배수로 끊음 [i:i+6]
    // for (int i=0; i<cnt; i++)
    // -----------------------
    // 1. #는 1로, *은 0으로 변환
    // ------------------------
    // 2. 문자를 숫자로 변환
    // 3. 이진수 -> 10진수
    // 4. 십진수 -> 아스키코드

    // 하나의 문자로 만들어주는 메서드
    /*public static void solution(int cnt, String str) {
        String subString;
        for (int i = 0; i < str.length(); i+=7) {
            for (char a :str.substring(i, i + 7).toCharArray() ) {
                //if (a.equals("#"==)) {

                }
            }
        }
    }*/
}

